{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install --no-audit --no-fund",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": "@vartalaap_api_url"
  },
  "build": {
    "env": {
      "NEXT_PUBLIC_API_BASE_URL": "@vartalaap_api_url"
    }
  }
}
