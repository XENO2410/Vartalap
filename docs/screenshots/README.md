# Screenshots

Drop your screenshots here and reference them from the top-level `README.md`.

Suggested files (already referenced from the README):

- `landing.png` — home page (bubble tabs + "वार्तालाप & Me")
- `chat.png` — chat with source cards and 👍/👎 feedback
- `mlflow-session.png` — MLflow session run overview
- `mlflow-traces.png` — Traces tab showing per-turn rows with eval tags
- `mlflow-metrics.png` — Model metrics tab with `turn.*` / `eval.*` charts
- `mlflow-system.png` — `system::vartalaap` run showing cross-session totals
